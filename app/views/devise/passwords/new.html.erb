<%= render "devise/shared/topbox" %>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>

  <div class="field">
    <%= f.label t('sb.mail') %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit t('sb.regen_password') %>
  </div>
<% end %>

</div>
</center>
